import React from 'react';
import { CardWrapper } from '../../../../sharedComponents/Card/CardStyle';
import PayrollListItem from './PayrollListItem';

const payrollList = () => {
    return (
        <CardWrapper >
            <PayrollListItem item={{}} />
            <PayrollListItem item={{}} />
            <PayrollListItem item={{}} />
            <PayrollListItem item={{}} />
            <PayrollListItem item={{}} />
            <PayrollListItem item={{}} />
            <PayrollListItem item={{}} />
        </CardWrapper>
    )
}
export default payrollList;